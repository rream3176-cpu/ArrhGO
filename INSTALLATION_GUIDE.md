# دليل التثبيت - ArhGo Trip Planner

## الخطوات المطلوبة بالضبط:

### الخطوة 1: تثبيت قاعدة البيانات

افتح **Command Prompt** أو **Terminal** واكتب:

```bash
mysql -u root -p < database/install_all.sql
```

**ملاحظة:** إذا كان لديك كلمة مرور لـ MySQL، سيطلب منك إدخالها.

**أو** إذا كنت تستخدم **phpMyAdmin**:
1. افتح phpMyAdmin
2. اضغط على "Import" (استيراد)
3. اختر ملف `database/install_all.sql`
4. اضغط "Go" (تنفيذ)

---

### الخطوة 2: إدراج البيانات (المدن، الفنادق، المطاعم)

بعد إنشاء قاعدة البيانات، قم بتشغيل:

```bash
mysql -u root -p < database/insert_cities.sql
mysql -u root -p < database/insert_hotels.sql
mysql -u root -p < database/insert_restaurants.sql
```

**أو في phpMyAdmin:**
- استورد الملفات بالترتيب:
  1. `insert_cities.sql`
  2. `insert_hotels.sql`
  3. `insert_restaurants.sql`

---

### الخطوة 3: تعديل إعدادات الاتصال

افتح ملف `api/config.php` وعدّل البيانات:

```php
define('DB_HOST', 'localhost');        // عادة localhost
define('DB_USER', 'root');              // اسم المستخدم (غالباً root)
define('DB_PASS', 'كلمة_المرور');      // كلمة مرور MySQL
define('DB_NAME', 'arhgo_trip_planner');
```

**مثال:**
```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', 'mypassword123');  // ضع كلمة مرورك هنا
define('DB_NAME', 'arhgo_trip_planner');
```

---

### الخطوة 4: اختبار الاتصال

افتح المتصفح واذهب إلى:
```
http://localhost/api/get_countries.php
```

**يجب أن ترى:** قائمة بالدول العربية في آسيا بصيغة JSON

---

### الخطوة 5: تحديث ملفات JavaScript

إذا كنت تريد استخدام قاعدة البيانات بدلاً من ملفات JavaScript:

#### أ. تحديث `assets/js/hotels-database.js`:

```javascript
// استبدل البيانات الثابتة بهذا الكود:
let hotelsDatabase = [];

// جلب البيانات من API
async function loadHotelsFromDB() {
    try {
        const response = await fetch('api/get_hotels.php');
        const data = await response.json();
        if (data.success) {
            hotelsDatabase = data.data;
        }
    } catch (error) {
        console.error('خطأ في جلب الفنادق:', error);
    }
}

// استدعاء الدالة عند تحميل الصفحة
loadHotelsFromDB();
```

#### ب. تحديث `assets/js/restaurants-database.js`:

```javascript
// استبدل البيانات الثابتة بهذا الكود:
let restaurantsDatabase = [];

// جلب البيانات من API
async function loadRestaurantsFromDB() {
    try {
        const response = await fetch('api/get_restaurants.php');
        const data = await response.json();
        if (data.success) {
            restaurantsDatabase = data.data;
        }
    } catch (error) {
        console.error('خطأ في جلب المطاعم:', error);
    }
}

// استدعاء الدالة عند تحميل الصفحة
loadRestaurantsFromDB();
```

---

## ملخص سريع:

### ✅ ما تم إنجازه:
1. ✅ قاعدة بيانات MySQL جاهزة
2. ✅ جداول: الدول، المدن، الفنادق، المطاعم
3. ✅ APIs PHP جاهزة
4. ✅ الفورم محدث (يعرض فقط الدول العربية في آسيا)

### 🔧 ما تحتاج فعله:
1. ✅ تثبيت قاعدة البيانات (الخطوة 1)
2. ✅ إدراج البيانات (الخطوة 2)
3. ✅ تعديل إعدادات الاتصال (الخطوة 3)
4. ✅ اختبار الاتصال (الخطوة 4)
5. ⚠️ (اختياري) تحديث ملفات JavaScript (الخطوة 5)

---

## استكشاف الأخطاء:

### مشكلة: "Access denied for user"
**الحل:** تأكد من اسم المستخدم وكلمة المرور في `api/config.php`

### مشكلة: "Unknown database"
**الحل:** تأكد من تشغيل `install_all.sql` أولاً

### مشكلة: APIs لا تعمل
**الحل:** 
- تأكد من تشغيل خادم PHP (XAMPP, WAMP, etc.)
- تأكد من أن ملفات API في مجلد `api/`
- تحقق من صلاحيات الملفات

### مشكلة: البيانات لا تظهر
**الحل:**
- تأكد من إدراج البيانات (insert_cities.sql, etc.)
- تحقق من الاتصال بقاعدة البيانات
- افتح Developer Tools في المتصفح وتحقق من الأخطاء

---

## اختبار سريع:

افتح المتصفح واذهب إلى:
- `http://localhost/api/get_countries.php` - يجب أن ترى 12 دولة
- `http://localhost/api/get_hotels.php?country_id=1` - يجب أن ترى فنادق السعودية
- `http://localhost/api/get_restaurants.php?country_id=1` - يجب أن ترى مطاعم السعودية

---

## ملاحظات مهمة:

1. **تأكد من تشغيل خادم PHP** (XAMPP, WAMP, Laragon, etc.)
2. **تأكد من تشغيل MySQL**
3. **الترتيب مهم:** قم بتشغيل الملفات بالترتيب المذكور
4. **النسخ الاحتياطي:** احتفظ بنسخة احتياطية من قاعدة البيانات

---

## الدعم:

إذا واجهت أي مشكلة، تحقق من:
1. ملف `database/README.md` للتفاصيل
2. سجلات الأخطاء في PHP
3. سجلات MySQL


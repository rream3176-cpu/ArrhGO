# 🧪 اختبار API الفنادق

## خطوات التشخيص:

### 1. اختبر API مباشرة:

افتح في المتصفح:
```
http://localhost/ArhGo-trip-planner/api/get_hotels.php
```

**يجب أن ترى:**
```json
{
  "success": true,
  "data": [...],
  "count": 76
}
```

**إذا رأيت:**
```json
{
  "success": true,
  "data": [],
  "count": 0
}
```
هذا يعني أن SQL query لا يعيد نتائج.

---

### 2. تحقق من JOIN:

المشكلة قد تكون في:
- `city_id` في جدول `hotels` لا يطابق `id` في جدول `cities`
- `country_id` في جدول `hotels` لا يطابق `id` في جدول `arab_countries_asia`

**الحل:** تم تغيير `INNER JOIN` إلى `LEFT JOIN` حتى يعرض الفنادق حتى لو لم تكن مرتبطة.

---

### 3. اختبر في phpMyAdmin:

نفذ هذا SQL:
```sql
SELECT 
    h.id,
    h.name,
    h.location,
    c.city_name_ar,
    a.country_name_ar
FROM hotels h
LEFT JOIN cities c ON h.city_id = c.id
LEFT JOIN arab_countries_asia a ON h.country_id = a.id
LIMIT 10;
```

**إذا لم تظهر نتائج:**
- تحقق من أن `city_id` و `country_id` في جدول `hotels` صحيحة

---

### 4. تحقق من Console:

1. افتح `F12` في المتصفح
2. افتح تبويب **Console**
3. جرب البحث
4. ابحث عن: `🔍 البحث عن الفنادق:` و `📦 النتيجة من API:`

---

## ✅ الحلول المطبقة:

1. ✅ تغيير `INNER JOIN` إلى `LEFT JOIN`
2. ✅ استخدام `COALESCE` للقيم الفارغة
3. ✅ تحسين معالجة `features` JSON

---

## 🔍 إذا استمرت المشكلة:

### تحقق من:

1. **قاعدة البيانات:**
   - اسم قاعدة البيانات: `arhgo1_trip_planner` ✅
   - جدول `hotels` يحتوي على بيانات ✅

2. **العلاقات:**
   - `hotels.city_id` → `cities.id`
   - `hotels.country_id` → `arab_countries_asia.id`

3. **API:**
   - افتح: `http://localhost/ArhGo-trip-planner/api/get_hotels.php`
   - يجب أن يعطي JSON

---

**بعد التحديثات، جرب البحث مرة أخرى!**


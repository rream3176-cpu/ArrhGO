# 🔧 حل مشكلة: ملفات PHP تعمل لكن HTML لا تعمل

## ⚠️ المشكلة:
- ✅ ملفات PHP تعمل: `http://localhost/ArhGo-trip-planner/api/get_hotels.php`
- ❌ ملفات HTML لا تعمل: `http://localhost/ArhGo-trip-planner/index.html`

**السبب:** تم نسخ مجلد `api` فقط وليس كل الملفات!

---

## ✅ الحل:

### الخطوة 1: انسخ جميع الملفات

#### أ) افتح المجلد الأصلي:
```
C:\Users\lenovo\OneDrive\Desktop\GPIS\ArhGo-trip-planner
```

#### ب) انسخ جميع الملفات:
1. حدد جميع الملفات (Ctrl+A)
2. انسخها (Ctrl+C)
3. اذهب إلى: `C:\wamp64\www\ArhGo-trip-planner\`
4. الصقها (Ctrl+V)

---

### الخطوة 2: تحقق من الملفات المطلوبة

بعد النسخ، يجب أن تجد في `C:\wamp64\www\ArhGo-trip-planner\`:

#### ملفات HTML:
- ✅ `index.html`
- ✅ `hotel.html`
- ✅ `Restuarnt.html`
- ✅ `login.html`
- ✅ `signup.html`
- ✅ جميع ملفات HTML الأخرى

#### مجلدات:
- ✅ `api/` (موجود - يعمل)
- ✅ `assets/` (CSS, JS, images)
- ✅ `database/` (SQL files)

---

### الخطوة 3: إذا كان المجلد غير موجود

#### أ) أنشئ المجلد:
1. اذهب إلى: `C:\wamp64\www\`
2. أنشئ مجلد جديد: `ArhGo-trip-planner`

#### ب) انسخ جميع الملفات:
1. من: `C:\Users\lenovo\OneDrive\Desktop\GPIS\ArhGo-trip-planner`
2. إلى: `C:\wamp64\www\ArhGo-trip-planner`

---

## 🚀 حل سريع (Copy-Paste):

### في File Explorer:

1. **افتح المجلد الأصلي:**
   ```
   C:\Users\lenovo\OneDrive\Desktop\GPIS\ArhGo-trip-planner
   ```

2. **حدد جميع الملفات:**
   - اضغط `Ctrl+A` (تحديد الكل)

3. **انسخ:**
   - اضغط `Ctrl+C` أو انقر بزر الماوس الأيمن → Copy

4. **الصق في WAMP:**
   - اذهب إلى: `C:\wamp64\www\ArhGo-trip-planner\`
   - إذا لم يكن موجوداً، أنشئه أولاً
   - اضغط `Ctrl+V` أو انقر بزر الماوس الأيمن → Paste

---

## 📋 قائمة التحقق:

بعد النسخ، تحقق من:

- [ ] المجلد موجود: `C:\wamp64\www\ArhGo-trip-planner`
- [ ] ملف `index.html` موجود
- [ ] ملف `hotel.html` موجود
- [ ] ملف `Restuarnt.html` موجود
- [ ] مجلد `api/` موجود (موجود - يعمل)
- [ ] مجلد `assets/` موجود
- [ ] WAMP يعمل (أيقونة خضراء)

---

## 🧪 اختبار:

بعد النسخ، جرب:

1. **الصفحة الرئيسية:**
   ```
   http://localhost/ArhGo-trip-planner/index.html
   ```

2. **صفحة الفنادق:**
   ```
   http://localhost/ArhGo-trip-planner/hotel.html
   ```

3. **صفحة المطاعم:**
   ```
   http://localhost/ArhGo-trip-planner/Restuarnt.html
   ```

---

## 🔍 استكشاف الأخطاء:

### إذا لم تعمل بعد:

#### 1. تحقق من WAMP:
- هل WAMP يعمل؟ (أيقونة خضراء)
- هل Apache نشط؟

#### 2. تحقق من المسار:
افتح PowerShell واكتب:
```powershell
Test-Path "C:\wamp64\www\ArhGo-trip-planner\index.html"
```
يجب أن يعطي: `True`

#### 3. تحقق من الأذونات:
- تأكد أن لديك صلاحيات القراءة على المجلد
- جرب فتح الملف مباشرة: `C:\wamp64\www\ArhGo-trip-planner\index.html`

---

## 📝 ملاحظة مهمة:

**⚠️ تذكر:** يجب نسخ **جميع الملفات** وليس فقط `api`!

- ✅ `api/` - موجود ويعمل
- ❌ `index.html` - غير موجود (يجب نسخه)
- ❌ `hotel.html` - غير موجود (يجب نسخه)
- ❌ `Restuarnt.html` - غير موجود (يجب نسخه)
- ❌ `assets/` - غير موجود (يجب نسخه)

---

## ✅ الحل النهائي:

**انسخ المجلد بالكامل:**
```
من: C:\Users\lenovo\OneDrive\Desktop\GPIS\ArhGo-trip-planner
إلى: C:\wamp64\www\ArhGo-trip-planner
```

**بعد النسخ، يجب أن تعمل جميع الملفات! 🎉**


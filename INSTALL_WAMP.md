# 🌐 دليل التنفيذ على WAMP - ArhGo Trip Planner

## 📋 المتطلبات
- ✅ WAMP مثبت ويعمل
- ✅ Apache و MySQL يعملان في WAMP
- ✅ متصفح إنترنت (Chrome, Firefox, Edge)

---

## 🚀 الخطوات بالتفصيل

### الخطوة 1: تشغيل WAMP

1. افتح **WAMP Server** (من قائمة Start أو Desktop)
2. انتظر حتى يصبح الأيقونة **خضراء** في شريط المهام
3. إذا كانت **برتقالية** → اضغط عليها واختر **Put Online**
4. إذا كانت **حمراء** → هناك مشكلة في الخوادم

---

### الخطوة 2: نقل المشروع إلى مجلد الخادم

#### أ) ابحث عن مجلد www:
- عادة يكون في: `C:\wamp64\www`
- أو: `C:\wamp\www` (إذا كان WAMP 32-bit)

#### ب) انسخ مجلد المشروع:
1. ابحث عن مجلد المشروع (على الأرجح في Desktop)
2. انسخ مجلد `ArhGo-trip-planner` بالكامل
3. الصقه في `C:\wamp64\www\`
4. المسار النهائي: `C:\wamp64\www\ArhGo-trip-planner`

**ملاحظة:** إذا كان WAMP 32-bit، استخدم `C:\wamp\www`

---

### الخطوة 3: فتح phpMyAdmin

1. اضغط على أيقونة WAMP في شريط المهام
2. اختر **phpMyAdmin**
3. أو افتح المتصفح واذهب إلى: `http://localhost/phpmyadmin`

---

### الخطوة 4: إنشاء قاعدة البيانات والجداول

#### أ) استيراد ملف إنشاء قاعدة البيانات:

1. في phpMyAdmin، اضغط على **Import** (استيراد) في القائمة العلوية
2. اضغط **Choose File** (اختر ملف)
3. اذهب إلى مجلد المشروع: `database\install_all.sql`
4. اختر الملف `install_all.sql`
5. تأكد من أن **Character set** هو `utf8mb4`
6. اضغط **Go** (تنفيذ) في الأسفل

#### ب) التحقق من النجاح:
- يجب أن ترى رسالة: "Import has been successfully finished"
- في القائمة الجانبية، يجب أن ترى قاعدة بيانات `arhgo_trip_planner`
- اضغط عليها لرؤية الجداول

---

### الخطوة 5: إدراج الدول (12 دولة)

1. في phpMyAdmin، اضغط على **Import** مرة أخرى
2. اضغط **Choose File**
3. اختر: `database\insert_countries.sql`
4. تأكد من أن قاعدة البيانات `arhgo_trip_planner` محددة في القائمة العلوية
5. اضغط **Go**

#### التحقق:
- اضغط على قاعدة البيانات `arhgo_trip_planner`
- اضغط على جدول `arab_countries_asia`
- اضغط **Browse** (تصفح)
- يجب أن ترى 12 دولة

---

### الخطوة 6: إدراج المدن (60+ مدينة)

1. اضغط **Import** في phpMyAdmin
2. اختر ملف: `database\insert_cities.sql`
3. تأكد من أن قاعدة البيانات `arhgo_trip_planner` محددة
4. اضغط **Go**

#### التحقق:
- اضغط على جدول `cities`
- اضغط **Browse**
- يجب أن ترى قائمة بالمدن

---

### الخطوة 7: إدراج الفنادق

1. اضغط **Import** في phpMyAdmin
2. اختر ملف: `database\insert_hotels.sql`
3. تأكد من أن قاعدة البيانات `arhgo_trip_planner` محددة
4. اضغط **Go**

#### التحقق:
- اضغط على جدول `hotels`
- اضغط **Browse**
- يجب أن ترى قائمة بالفنادق

---

### الخطوة 8: إدراج المطاعم

1. اضغط **Import** في phpMyAdmin
2. اختر ملف: `database\insert_restaurants.sql`
3. تأكد من أن قاعدة البيانات `arhgo_trip_planner` محددة
4. اضغط **Go**

#### التحقق:
- اضغط على جدول `restaurants`
- اضغط **Browse**
- يجب أن ترى قائمة بالمطاعم

---

### الخطوة 9: التحقق من إعدادات الاتصال

1. افتح ملف: `api\config.php` في محرر النصوص
2. تأكد من الإعدادات التالية:

```php
define('DB_HOST', 'localhost');
define('DB_USER', 'root');
define('DB_PASS', '');  // فارغة في WAMP عادة
define('DB_NAME', 'arhgo_trip_planner');
```

3. احفظ الملف

---

### الخطوة 10: اختبار API

افتح الروابط التالية في المتصفح للتحقق من عمل API:

#### 1. اختبار الدول:
```
http://localhost/ArhGo-trip-planner/api/get_countries.php
```
**النتيجة المتوقعة:** يجب أن ترى JSON يحتوي على 12 دولة

#### 2. اختبار المدن (السعودية):
```
http://localhost/ArhGo-trip-planner/api/get_cities.php?country_id=1
```
**النتيجة المتوقعة:** يجب أن ترى مدن السعودية

#### 3. اختبار الفنادق (السعودية):
```
http://localhost/ArhGo-trip-planner/api/get_hotels.php?country_id=1
```
**النتيجة المتوقعة:** يجب أن ترى فنادق السعودية

#### 4. اختبار المطاعم (السعودية):
```
http://localhost/ArhGo-trip-planner/api/get_restaurants.php?country_id=1
```
**النتيجة المتوقعة:** يجب أن ترى مطاعم السعودية

---

### الخطوة 11: فتح المشروع

افتح المتصفح واذهب إلى:
```
http://localhost/ArhGo-trip-planner/index.html
```

**يجب أن ترى:**
- ✅ الصفحة الرئيسية تعمل
- ✅ قائمة الدول تعرض 12 دولة عربية
- ✅ لا توجد أخطاء في Console (F12)

---

## ✅ قائمة التحقق النهائية

- [ ] WAMP يعمل (أيقونة خضراء)
- [ ] Apache و MySQL يعملان
- [ ] المشروع في `C:\wamp64\www\ArhGo-trip-planner` (أو `C:\wamp\www`)
- [ ] قاعدة البيانات `arhgo_trip_planner` موجودة
- [ ] جدول `arab_countries_asia` يحتوي على 12 دولة
- [ ] جدول `cities` يحتوي على مدن
- [ ] جدول `hotels` يحتوي على فنادق
- [ ] جدول `restaurants` يحتوي على مطاعم
- [ ] API يعمل (جميع الروابط تعرض JSON)
- [ ] الموقع يفتح بدون أخطاء

---

## 🔧 حل المشاكل الشائعة

### مشكلة: WAMP أيقونة حمراء
**الحل:**
- تأكد من عدم وجود برامج أخرى تستخدم Port 80 (مثل Skype)
- أعد تشغيل WAMP كـ Administrator
- تحقق من أن Apache و MySQL يعملان

### مشكلة: "Not Found" عند فتح الموقع
**الحل:**
- تأكد من أن المشروع في `C:\wamp64\www\ArhGo-trip-planner`
- تأكد من أن Apache يعمل
- جرب: `http://localhost/ArhGo-trip-planner/`
- تحقق من اسم المجلد بالضبط

### مشكلة: "Connection failed" في API
**الحل:**
- تأكد من أن MySQL يعمل في WAMP
- تحقق من إعدادات `api/config.php`
- تأكد من أن قاعدة البيانات موجودة
- جرب إعادة تشغيل MySQL من WAMP

### مشكلة: "Foreign key constraint" عند الاستيراد
**الحل:**
- تأكد من تنفيذ الملفات بالترتيب:
  1. `install_all.sql`
  2. `insert_countries.sql`
  3. `insert_cities.sql`
  4. `insert_hotels.sql`
  5. `insert_restaurants.sql`

### مشكلة: البيانات لا تظهر بالعربية
**الحل:**
- في phpMyAdmin، عند الاستيراد، اختر **Character set: utf8mb4**
- تأكد من أن قاعدة البيانات تستخدم `utf8mb4_unicode_ci`

### مشكلة: phpMyAdmin لا يفتح
**الحل:**
- تأكد من أن Apache يعمل
- جرب: `http://127.0.0.1/phpmyadmin`
- اضغط على أيقونة WAMP واختر **phpMyAdmin**

---

## 📍 المسارات المهمة في WAMP

### WAMP 64-bit:
- **www folder:** `C:\wamp64\www`
- **phpMyAdmin:** `http://localhost/phpmyadmin`
- **Apache config:** `C:\wamp64\bin\apache\apache[version]\conf\httpd.conf`
- **MySQL data:** `C:\wamp64\bin\mysql\mysql[version]\data`

### WAMP 32-bit:
- **www folder:** `C:\wamp\www`
- **phpMyAdmin:** `http://localhost/phpmyadmin`
- **Apache config:** `C:\wamp\bin\apache\apache[version]\conf\httpd.conf`
- **MySQL data:** `C:\wamp\bin\mysql\mysql[version]\data`

---

## 🎯 روابط سريعة

بعد التثبيت، استخدم هذه الروابط:

- **الموقع الرئيسي:**
  ```
  http://localhost/ArhGo-trip-planner/index.html
  ```

- **phpMyAdmin:**
  ```
  http://localhost/phpmyadmin
  ```

- **API - الدول:**
  ```
  http://localhost/ArhGo-trip-planner/api/get_countries.php
  ```

- **API - المدن:**
  ```
  http://localhost/ArhGo-trip-planner/api/get_cities.php?country_id=1
  ```

---

## 🎉 تهانينا!

إذا وصلت إلى هنا وكل شيء يعمل، فأنت جاهز لاستخدام ArhGo Trip Planner!

**الخطوة التالية:** ابدأ بتخطيط رحلتك! 🗺️✈️

---

## 📞 ملاحظات إضافية

- **مسار المشروع:** `C:\wamp64\www\ArhGo-trip-planner` (أو `C:\wamp\www`)
- **رابط الموقع:** `http://localhost/ArhGo-trip-planner/index.html`
- **رابط phpMyAdmin:** `http://localhost/phpmyadmin`
- **اسم قاعدة البيانات:** `arhgo_trip_planner`

---

**تم إنشاء هذا الدليل خصيصاً لـ WAMP Server! 🌐**


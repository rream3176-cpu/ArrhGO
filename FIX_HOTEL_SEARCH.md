# إصلاح مشكلة البحث عن الفنادق

## الخطوات التشخيصية

### 1. تحقق من قاعدة البيانات

افتح **phpMyAdmin** (`http://localhost/phpmyadmin`) وتحقق:

```sql
-- عدد الفنادق
SELECT COUNT(*) as total FROM hotels;

-- عرض أول 5 فنادق
SELECT id, name, location, budget, rating, price FROM hotels LIMIT 5;
```

**إذا كانت النتيجة 0** → المشكلة في قاعدة البيانات (لم يتم إدخال البيانات)

**إذا كانت النتيجة > 0** → المشكلة في API أو JavaScript

---

### 2. اختبر API مباشرة

افتح في المتصفح:
```
http://localhost/ArhGo-trip-planner/api/get_hotels.php
```

**يجب أن ترى JSON مثل:**
```json
{
    "success": true,
    "data": [...],
    "count": 76,
    "total_in_db": 76
}
```

**إذا رأيت خطأ** → المشكلة في `api/config.php` أو الاتصال بقاعدة البيانات

**إذا رأيت `"count": 0`** → المشكلة في الاستعلام SQL

---

### 3. اختبر صفحة الاختبار

افتح في المتصفح:
```
http://localhost/ArhGo-trip-planner/api/test_hotels.php
```

**يجب أن ترى:**
- عدد الفنادق في قاعدة البيانات
- جدول بأول 5 فنادق

---

### 4. تحقق من Console في المتصفح

1. افتح `http://localhost/ArhGo-trip-planner/hotel.html`
2. اضغط `F12` → افتح تبويب **Console**
3. اضغط "بحث عن فنادق" (اترك الحقول فارغة)
4. راقب الرسائل:

**يجب أن ترى:**
```
🔍 البحث عن الفنادق: api/get_hotels.php
📦 النتيجة من API: {...}
📊 عدد الفنادق: 76
✅ API نجح، عدد الفنادق: 76
🏨 الفنادق بعد التحويل: [...]
🎨 renderHotels استدعي مع 76 فندق
```

**إذا رأيت خطأ** → انسخ الرسالة وأرسلها

---

### 5. تحقق من Network Tab

1. افتح `F12` → تبويب **Network**
2. اضغط "بحث عن فنادق"
3. اضغط على `get_hotels.php`
4. افتح تبويب **Response**

**يجب أن ترى JSON مع البيانات**

**إذا رأيت خطأ** → انسخ الرسالة وأرسلها

---

## الحلول الشائعة

### المشكلة: قاعدة البيانات فارغة

**الحل:**
1. افتح phpMyAdmin
2. تأكد من أن قاعدة البيانات `arhgo1_trip_planner` موجودة
3. تأكد من أن جدول `hotels` موجود
4. نفذ ملفات SQL:
   - `database/insert_hotels.sql`

---

### المشكلة: خطأ في الاتصال بقاعدة البيانات

**الحل:**
1. افتح `api/config.php`
2. تأكد من:
   ```php
   define('DB_NAME', 'arhgo1_trip_planner');
   ```
3. تأكد من أن WAMP/XAMPP يعمل
4. تأكد من أن MySQL يعمل

---

### المشكلة: API يعيد `count: 0` رغم وجود البيانات

**الحل:**
1. افتح `api/get_hotels.php`
2. تحقق من أن الاستعلام SQL صحيح
3. جرب الاستعلام مباشرة في phpMyAdmin:
   ```sql
   SELECT * FROM hotels WHERE 1=1 ORDER BY rating DESC, price ASC LIMIT 100;
   ```

---

### المشكلة: JavaScript لا يعرض الفنادق

**الحل:**
1. افتح Console (`F12`)
2. تحقق من وجود أخطاء JavaScript
3. تحقق من أن `result.success === true`
4. تحقق من أن `result.data` يحتوي على مصفوفة

---

## أرسل لي:

1. **عدد الفنادق في قاعدة البيانات** (من phpMyAdmin)
2. **ماذا ترى عند فتح `api/get_hotels.php` مباشرة؟**
3. **ماذا يظهر في Console عند البحث؟**
4. **ماذا يظهر في Network → Response؟**

---

## ملاحظة مهمة:

تأكد من أن:
- ✅ WAMP/XAMPP يعمل
- ✅ MySQL يعمل
- ✅ قاعدة البيانات `arhgo1_trip_planner` موجودة
- ✅ جدول `hotels` موجود ويحتوي على بيانات
- ✅ الملفات موجودة في `C:\wamp64\www\ArhGo-trip-planner\`
